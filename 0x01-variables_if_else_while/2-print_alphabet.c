#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: A program to print alphabets
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char a;

	for (a = 'a' ; a <= 'z' ; a++)
	{
		putchar(a);
	}
	return (0);
}
