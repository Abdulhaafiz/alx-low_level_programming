#include <stdio.h>

/**
 *main - Entry point
 *
 * Description: A program to print numbers
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a;

	for (a = 1 ; a < 10 ; a++)
	{
		putchar(a);
	}
	return (0);
}
