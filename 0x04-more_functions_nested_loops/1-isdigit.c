#include "main.h"

/**
 * _isdigit - A function that checks digits from 0 - 9
 *
 * @c: The number
 *
 * Return: 1 if c is a digit else 0
 */

int _isdigit(int c)
{
	if (c == 0 && c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
